[build]
dockerfilePath = "apps/api/Dockerfile"
buildContext = "."

[deploy]
startCommand = "sh -c 'npx tsx packages/db/src/migrate.ts && node apps/api/dist/server.js'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
