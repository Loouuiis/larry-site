# Larry Worker (`apps/worker`)

BullMQ worker for async job processing (Stage 1).

## Run

From repo root:

```bash
npm run worker:dev
```

## Build / Start

```bash
npm run worker:build
npm run worker:start
```

## Env

Copy `apps/worker/.env.example` to `apps/worker/.env`.
