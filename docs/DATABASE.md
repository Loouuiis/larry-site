# Larry - Database

## Connection

- Schema: `packages/db/src/schema.sql`
- Migration runner: `packages/db/src/migrate.ts`
- Seed: `packages/db/src/seed.ts`
- Client: `packages/db/src/index.ts`

```bash
cd packages/db
npm run migrate
npm run seed
```

## Key Tables

### Tenancy and Identity
| Table | Purpose |
|-------|---------|
| `tenants` | Tenant boundary |
| `users` | User accounts |
| `memberships` | Tenant membership and role |
| `org_invites` | Org request/approval flow |
| `refresh_tokens` | JWT refresh token store with revocation tracking and device metadata (`ip_address`, `user_agent`) |
| `password_reset_tokens` | Password reset token hashes with expiry and usage tracking |
| `email_verification_tokens` | Email verification token hashes with expiry and usage tracking |
| `email_change_requests` | Pending email-address change requests with confirmation token and expiry |
| `login_attempts` | Per-user login attempt counter with account lock-until timestamp (brute-force protection) |
| `user_oauth_accounts` | Linked OAuth provider accounts (`provider`, `provider_user_id`) per user |

### Workspace Core
| Table | Purpose |
|-------|---------|
| `projects` | Project records (`active`/`archived` lifecycle, `larry_context` for agent memory) |
| `project_memberships` | Project-scoped collaborator membership (`owner` / `editor` / `viewer`) |
| `project_notes` | Shared and personal project collaborator notes |
| `project_intake_drafts` | Durable unified intake drafts (manual/chat/meeting, bootstrap preview, finalization metadata) |
| `tasks` | Project tasks (includes `assigned_to_larry`, `completed_by_larry`, `larry_document_id` for agent-executed work) |
| `task_dependencies` | Task dependency graph |
| `task_comments` | Task-level discussion |
| `documents` | Project document assets (`doc_type`, source linkage, version, metadata, `folder_id`) |
| `task_document_attachments` | Task to document linkage table |
| `meeting_notes` | Meeting transcript/summary records |
| `folders` | Hierarchical folder tree for documents (`project`/`company`/`general` types, max depth 4, self-referencing `parent_id`) |

### Canonical Larry Runtime
| Table | Purpose |
|-------|---------|
| `raw_events` | Deduplicated raw ingest payloads from external sources (idempotency-keyed) |
| `canonical_events` | Normalized source ingest events (references `raw_events`) |
| `canonical_event_processing_attempts` | Runtime reliability ledger for canonical event queue attempts (`running`, `succeeded`, `retryable_failed`, `dead_lettered`) |
| `larry_conversations` | Conversation sessions |
| `larry_messages` | Message history with actor attribution |
| `larry_events` | Action-centre ledger with lifecycle/provenance (execution mode, source tracking, approval/dismissal audit) |
| `larry_briefings` | Generated briefing payloads |
| `larry_rules` | Tenant-scoped custom behavioural rules for Larry (`rule_type`, active/inactive toggle) |
| `larry_documents` | Files generated by Larry (email drafts, letters, memos, reports) with state lifecycle (`draft`/`final`/`sent`) and optional `folder_id` |
| `project_memory_entries` | Per-project memory facts extracted from signals (deduplicated by `content_hash`, `source_kind` normalized) |
| `correction_feedback` | User correction records |

### Connectors and Messaging
| Table | Purpose |
|-------|---------|
| `slack_installations` | Slack OAuth installation metadata |
| `slack_channel_project_mappings` | Slack channel to project mapping |
| `google_calendar_installations` | Google Calendar OAuth/watch metadata with optional default project linkage |
| `outlook_calendar_installations` | Outlook Calendar OAuth metadata with subscription/webhook tracking (`outlook_subscription_id`, expiration) |
| `email_installations` | Email connector installation metadata |
| `email_outbound_drafts` | Outbound draft records |

### Audit and Reporting
| Table | Purpose |
|-------|---------|
| `audit_log` | Immutable audit events (hash-chained) |
| `activity_log` | Project/task activity feed events with actor attribution and JSONB payload |
| `notifications` | User notifications with per-scope deduplication and `read_at` tracking |
| `risk_snapshots` | Risk snapshot history |
| `report_snapshots` | Reporting snapshots |
| `kpi_snapshots` | Periodic KPI metric values per project (`metric_key`, `period_start`/`period_end`) |
| `tenant_policy_settings` | Tenant-level policy configuration (confidence thresholds, `autonomy_level` 1-5) |

## Tenant Isolation Pattern

All product tables include `tenant_id`. Row Level Security (RLS) is enabled on every product table and enforced via policies that compare `tenant_id::text` against `current_setting('app.tenant_id', true)`. The application must `SET app.tenant_id` before issuing queries.

Selected connector tables (`slack_installations`, `google_calendar_installations`, `email_installations`) also have a `__system__` lookup policy to allow cross-tenant webhook routing.

Identity tables without a `tenant_id` column (`users`, `password_reset_tokens`, `email_verification_tokens`, `user_oauth_accounts`, `email_change_requests`, `login_attempts`) are not RLS-guarded -- they are accessed through authenticated user context rather than tenant scoping.

Use tenant-aware query helpers consistently in API and worker code.

## Migration Notes

- Run schema migration before API/worker deployment.
- Restart API and worker after schema changes.
- For remote databases, validate connectivity before migration execution.
- `project_memberships` includes:
  - primary key `(tenant_id, project_id, user_id)`
  - role check constraint (`owner|editor|viewer`)
  - indexes on `(tenant_id, project_id, updated_at)` and `(tenant_id, user_id, updated_at)`
  - tenant RLS policy (`tenant_isolation_project_memberships`)
  - idempotent backfill:
    - project owner -> `owner`
    - existing tenant members on each project -> `viewer`
- `project_notes` includes:
  - visibility check (`shared|personal`)
  - recipient constraint: shared notes require `recipient_user_id IS NULL`; personal notes require non-null recipient
  - indexes on `(tenant_id, project_id, created_at)` and `(tenant_id, recipient_user_id, created_at)`
  - tenant RLS policy (`tenant_isolation_project_notes`)
- `project_intake_drafts` includes tenant RLS policy (`tenant_isolation_project_intake_drafts`) and recency/status/mode indexes for intake runtime access paths.
- `documents` includes additive asset fields (`source_kind`, `source_record_id`, `version`, `metadata`, `updated_at`) and recency/source lookup indexes:
  - `idx_documents_tenant_project_updated`
  - `idx_documents_tenant_doc_type_updated`
  - `idx_documents_tenant_source`
- `task_document_attachments` includes:
  - uniqueness guard `(tenant_id, task_id, document_id)`
  - recency indexes for task/document lookups
  - tenant RLS policy (`tenant_isolation_task_document_attachments`)
- `google_calendar_installations` includes additive default-linkage support:
  - nullable `project_id` (`ON DELETE SET NULL`)
  - tenant/project recency lookup index `idx_google_calendar_installations_tenant_project`
- `canonical_event_processing_attempts` includes additive runtime-reliability coverage:
  - unique guard `(tenant_id, canonical_event_id, attempt_number)`
  - bounded status check (`running|succeeded|retryable_failed|dead_lettered`)
  - recency/filter indexes:
    - `idx_canonical_event_processing_attempts_tenant_status_started`
    - `idx_canonical_event_processing_attempts_tenant_source_status_started`
    - `idx_canonical_event_processing_attempts_tenant_canonical_attempt`
  - tenant RLS policy (`tenant_isolation_canonical_event_processing_attempts`)
- `projects.status` is hardened to the archive lifecycle enum surface:
  - stored values are constrained to `active|archived`
  - additive normalization backfills unexpected/null values to `active`
  - archive-aware recency index: `idx_projects_tenant_status_recent (tenant_id, status, updated_at DESC, created_at DESC)`
- Project hard-delete runtime contract (`POST /v1/projects/:id/delete`) explicitly purges project-linked rows from:
  - `meeting_notes`
  - `documents`
  - `email_outbound_drafts`
  - `larry_conversations`
  before deleting the `projects` row. This avoids leaving retained `ON DELETE SET NULL` artifacts for hard-deleted projects.
- Legacy compatibility columns remain in schema (`meeting_notes.agent_run_id`, `email_outbound_drafts.action_id`), but runtime insert paths no longer explicitly write `NULL` into those columns.
- Seed data includes one deterministic `project_intake_drafts` fixture row so local/demo environments exercise the new intake contract.
- Seed data includes deterministic `project_memberships` rows (including a multi-user project) so collaboration access is visible in local/demo environments.
- Seed data includes deterministic `project_notes` rows (shared and personal) for workspace-note demos.
- Seed data includes deterministic document assets (`email_draft`, `docx_template`, `xlsx_template`) and one task-document attachment row.
- Seed data includes deterministic `canonical_event_processing_attempts` fixtures for one retryable-failed and one dead-lettered canonical event example.
- Seed data now includes one deterministic archived project fixture with:
  - collaborator memberships
  - one archived task
  - one archived meeting note
- `larry_rules` includes:
  - tenant RLS policy (`tenant_isolation_larry_rules`)
  - active-filter index: `idx_larry_rules_tenant (tenant_id, is_active)`
- `larry_documents` includes:
  - typed document lifecycle: `doc_type` check (`email_draft|letter|memo|report|note|other`), `state` check (`draft|final|sent`)
  - optional linkage to `larry_events`, `projects`, `folders`, and `users`
  - tenant RLS policy (`larry_documents_tenant_isolation`)
  - indexes on `(tenant_id, created_at)`, `(project_id, created_at)`, `(larry_event_id)`, `(tenant_id, folder_id)`
- `folders` includes:
  - self-referencing hierarchy via `parent_id` with max `depth` of 4
  - folder type check (`project|company|general`)
  - tenant RLS policy (`tenant_isolation_folders`)
  - indexes on `(tenant_id, parent_id, sort_order)` and `(tenant_id, project_id)`
  - backfill: auto-creates a `general` folder per tenant and a root `project` folder per project
  - backfill: moves existing `documents` and `larry_documents` into their project's root folder (or `general` folder for orphans)
- `outlook_calendar_installations` includes:
  - unique guard `(tenant_id, outlook_calendar_id)`
  - subscription tracking columns: `outlook_subscription_id`, `outlook_subscription_client_state`, `outlook_subscription_expiration`
  - tenant RLS policy (`tenant_isolation_outlook_calendar_installations`)
- `project_memory_entries` includes:
  - content deduplication via `content_hash` (SHA-256) and unique replay-dedup index `(tenant_id, project_id, source_kind, source_record_id, content_hash)`
  - normalized `source_kind` values (`meeting`, `email`, `slack`, `calendar`)
  - tenant RLS policy (`tenant_isolation_project_memory_entries`)
- `email_change_requests` includes:
  - token-hash and user indexes for lookup
  - `confirmed_at` timestamp for completion tracking
- `login_attempts` includes:
  - primary key on `user_id` (one row per user)
  - `attempt_count`, `locked_until`, `last_attempt_at` for brute-force lockout
- `tasks` gained Larry-executor columns: `assigned_to_larry`, `completed_by_larry`, `larry_document_id` (FK to `larry_documents`) with partial indexes for agent-assigned/completed lookups
- `refresh_tokens` gained device metadata columns: `ip_address`, `user_agent`
